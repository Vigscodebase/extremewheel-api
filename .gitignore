node_modules
*.log
server.log

# environment files — never commit real secrets, .env.example is the template
.env
.env.*
!.env.example

# RSA keypair generated by generate-keys.js — must never be committed
*.pem
